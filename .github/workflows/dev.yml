name: Deploiement SmartHotel360

on:
  push:
    branches:
      - dev

env:
  AZURE_RG: RG-Smart-Hotel
  AZURE_LOCATION: westeurope
  AZURE_PLAN_NAME: smh360plan
  AZURE_WEB_APP_NAME: smh360webd4040cc
  AZURE_DB_SERVER: smh360mysql40405cc
  AZURE_DB_ADMIN: sqladmin
  AZURE_DB_PW: P2ssw0rd@123
  AZURE_DB_NAME: hotel_coupon

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: repo
        uses: actions/checkout@v2

      - name: Setup Java
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Creation du package ROOT.war
        run: mvn --batch-mode --update-snapshots verify

      - name: copie des artefacts
        run: mkdir staging && cp target/*.war staging

      - name: Publication des artefacts  
        uses: actions/upload-artifact@v2
        with:
          name: Package
          path: staging

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Azure Login
        uses: Azure/login@v1
        with:    
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Recuperation des sources - Git
        uses: actions/checkout@v2    

      - name: Installation Module Azure pour Ansible
        run: |
          wget https://raw.githubusercontent.com/ansible-collections/azure/dev/requirements-azure.txt
          pip3 install -r requirements-azure.txt
          ansible-galaxy collection install azure.azcollection

      - name: Execution du plabook Ansible
        run: |    
          ansible-playbook ./ansible/main.yml --extra-vars "resource_group=${{ env.AZURE_RG }} location=${{ env.AZURE_LOCATION }} webapp_name=${{ env.AZURE_WEB_APP_NAME }} appplan_name= ${{ env.AZURE_PLAN_NAME }} mysqlserver_name=${{ env.AZURE_DB_SERVER }} admin_username=${{ env.AZURE_DB_ADMIN }} admin_password=${{ env.AZURE_DB_PW }} mysqldb_name=${{ env.AZURE_DB_NAME }}"        